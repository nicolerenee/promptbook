// sync.js — placeholder. Implemented in a later wave.
(function () {
  'use strict';
  var root = document.getElementById('page-root');
  if (!root || root.getAttribute('data-page') !== 'sync') return;
  console.log('TODO: implement sync page');
  root.innerHTML = '<div class="pb-empty">' +
    'This page is being rebuilt in the new design. Coming soon.' +
    '</div>';
})();
