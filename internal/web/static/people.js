// people.js — placeholder. Implemented in a later wave.
(function () {
  'use strict';
  var root = document.getElementById('page-root');
  if (!root || root.getAttribute('data-page') !== 'people') return;
  console.log('TODO: implement people page');
  root.innerHTML = '<div class="pb-empty">' +
    'This page is being rebuilt in the new design. Coming soon.' +
    '</div>';
})();
