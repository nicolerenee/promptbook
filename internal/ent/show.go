// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/nicolerenee/promptbook/internal/ent/show"
)

// Show is the model entity for the Show schema.
type Show struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// DescriptionHTML holds the value of the "description_html" field.
	DescriptionHTML string `json:"description_html,omitempty"`
	// LastSeenAt holds the value of the "last_seen_at" field.
	LastSeenAt time.Time `json:"last_seen_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShowQuery when eager-loading is set.
	Edges        ShowEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ShowEdges holds the relations/edges for other nodes in the graph.
type ShowEdges struct {
	// Recordings holds the value of the recordings edge.
	Recordings []*Recording `json:"recordings,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedRecordings map[string][]*Recording
}

// RecordingsOrErr returns the Recordings value or an error if the edge
// was not loaded in eager-loading.
func (e ShowEdges) RecordingsOrErr() ([]*Recording, error) {
	if e.loadedTypes[0] {
		return e.Recordings, nil
	}
	return nil, &NotLoadedError{edge: "recordings"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Show) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case show.FieldID:
			values[i] = new(sql.NullInt64)
		case show.FieldName, show.FieldDescriptionHTML:
			values[i] = new(sql.NullString)
		case show.FieldLastSeenAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Show fields.
func (_m *Show) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case show.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case show.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case show.FieldDescriptionHTML:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description_html", values[i])
			} else if value.Valid {
				_m.DescriptionHTML = value.String
			}
		case show.FieldLastSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_at", values[i])
			} else if value.Valid {
				_m.LastSeenAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Show.
// This includes values selected through modifiers, order, etc.
func (_m *Show) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRecordings queries the "recordings" edge of the Show entity.
func (_m *Show) QueryRecordings() *RecordingQuery {
	return NewShowClient(_m.config).QueryRecordings(_m)
}

// Update returns a builder for updating this Show.
// Note that you need to call Show.Unwrap() before calling this method if this Show
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Show) Update() *ShowUpdateOne {
	return NewShowClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Show entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Show) Unwrap() *Show {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Show is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Show) String() string {
	var builder strings.Builder
	builder.WriteString("Show(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description_html=")
	builder.WriteString(_m.DescriptionHTML)
	builder.WriteString(", ")
	builder.WriteString("last_seen_at=")
	builder.WriteString(_m.LastSeenAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NamedRecordings returns the Recordings named value or an error if the edge was not
// loaded in eager-loading with this name.
func (_m *Show) NamedRecordings(name string) ([]*Recording, error) {
	if _m.Edges.namedRecordings == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := _m.Edges.namedRecordings[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (_m *Show) appendNamedRecordings(name string, edges ...*Recording) {
	if _m.Edges.namedRecordings == nil {
		_m.Edges.namedRecordings = make(map[string][]*Recording)
	}
	if len(edges) == 0 {
		_m.Edges.namedRecordings[name] = []*Recording{}
	} else {
		_m.Edges.namedRecordings[name] = append(_m.Edges.namedRecordings[name], edges...)
	}
}

// Shows is a parsable slice of Show.
type Shows []*Show
